# 域名安全与国内访问解决方案

## 问题说明

当前网站部署在 Cloudflare Pages，使用默认域名 `garment-pattern-app.pages.dev`，在中国大陆遇到两个问题：

1. **微信拦截**：点击链接直接提示安全风险，需提交证明材料和部署验证文件
2. **浏览器风险提示**：华为等国产浏览器提示"该网站可能存在风险 / 恶意网址拦截"

## 根本原因

`pages.dev` 是 Cloudflare 的**共享公共域名**，任何人都可以零成本注册子域名。大量钓鱼、诈骗网站也使用此域名，导致微信和国产浏览器将整个 `pages.dev` 父域加入黑名单。这是"连坐"机制——你的网站内容没有问题，但被其他违规子域名牵连。

> 此问题无法通过修改代码解决，必须更换为自有域名。

## 解决方案

### 方案一：自有域名 + Cloudflare Pages（推荐，个人项目首选）

成本最低、见效最快，约 9-30 元/年。

#### 步骤 1：购买独立域名

在阿里云/华为云/Namesilo 注册一个域名，例如：
- `.online` / `.top` / `.xyz` — 约 9-15 元/年（最便宜）
- `.com` — 约 55-70 元/年

> 避免使用 `.dev` 后缀，它和 `pages.dev` 一样容易被标记。

#### 步骤 2：将域名 DNS 托管到 Cloudflare

1. 登录 [Cloudflare 控制台](https://dash.cloudflare.com)，添加你的域名
2. Cloudflare 会给你两个 DNS 服务器地址（如 `xxx.ns.cloudflare.com`）
3. 到域名注册商处，将 DNS 服务器改为 Cloudflare 提供的地址
4. 等待 DNS 生效（通常几分钟到几小时）

#### 步骤 3：在 Cloudflare Pages 绑定自定义域名

1. 进入 Cloudflare Pages 项目 → 自定义域 → 设置自定义域
2. 输入你的域名（如 `garment-pattern-app.example.com`）
3. Cloudflare 自动添加 CNAME 记录并签发 SSL 证书
4. 等待几分钟后域名生效

#### 步骤 4：解决微信拦截 — 站长认证

1. 在微信内打开你的**新域名**网址
2. 如遇拦截提示，点击「申请恢复访问」→「我是站长」
3. 微信给出一个文件名和文件内容
4. 在项目的 `public/` 目录下创建同名的 TXT 文件，填入微信给的内容
5. 重新部署网站（git push 到 GitHub，Cloudflare Pages 自动构建）
6. 部署完成后，验证文件可通过 `https://你的域名/文件名.txt` 访问
7. 回到微信页面点击「已部署，开始验证」

> 注意：必须使用自有域名进行认证。用 pages.dev 申诉会被拒绝，因为微信是按域名级封禁的。

#### 步骤 5：解决华为浏览器拦截

绑定自有域名后，`pages.dev` 的黑名单不再影响你。如仍有残留提示：

- 发送邮件到 `gjfzzx@cert.org.cn` 和 `hwyfw@mail01.huawei.com`
- 提供域名、ICP备案号（如有）、联系方式
- 或致电 `010-67825170`

---

### 方案二：国内云 + ICP备案（商业项目推荐）

完全合规，所有平台无任何拦截。

#### 步骤

1. 在阿里云/腾讯云购买 `.cn` 或 `.com` 域名（需实名认证）
2. 购买云服务器 ECS（轻量应用服务器即可，约 24-60 元/月）
3. 提交 ICP备案（约 7-20 个工作日）
4. 备案通过后，使用腾讯云 COS / 阿里云 OSS 静态网站托管
5. 将域名 CNAME 到 COS/OSS 桶地址
6. 在微信、华为浏览器等所有平台均可正常访问

---

### 方案三：腾讯云 EdgeOne Pages（折中方案）

无需备案，国内访问速度最快。

1. 登录 [腾讯云 EdgeOne 控制台](https://console.cloud.tencent.com/edgeone)
2. 选择「Pages 服务」→ 从 Git 仓库导入
3. 构建命令 `npm run build`，输出目录 `dist`
4. 获得 `*.eo.edgeone.app` 域名，大陆可直接访问
5. 可选：绑定自有域名进一步提升可信度

> EdgeOne 是腾讯云服务，域名信誉较好，被拦截概率低于 pages.dev。

---

## 方案对比

| 方案 | 成本 | 备案 | 微信可用 | 国内速度 | 推荐场景 |
|------|------|------|---------|---------|---------|
| 自有域名 + CF Pages | 9-30元/年 | 否 | 需站长认证 | 良好 | 个人项目 |
| 国内云 + ICP备案 | 100-300元/年 | 是 | 完全合规 | 优秀 | 商业项目 |
| 腾讯云 EdgeOne | 免费 | 否 | 基本可用 | 最快 | 折中方案 |

## 关于 public/ 目录

本项目的 `public/` 目录用于放置微信站长认证所需的验证文件。Vite 构建时会将此目录下的所有文件原样复制到 `dist/` 根目录，部署后可通过 `https://你的域名/文件名.txt` 直接访问。

详细使用说明见 `public/README-WECHAT-VERIFY.txt`。
